# 일일 개발 보고서 — 2026-04-17

## 오늘의 요약
AI Hub 격상을 위한 **첫 프론트엔드 구현 세션**을 완료하여 Agent Store 페이지, Workspace-Agent 연결 UI, 알림 시스템 분리를 마쳤다. 동시에 워크스페이스 컨텍스트 유지, SAP 다중 시스템 지원, PDF 폰트 버그, VOC 담당자 필터링, Bedrock 폴백 영속화, MCP 동시 스폰 제한 등 **6개 백엔드 버그 수정**을 진행하여 안정성을 대폭 개선했다.

---

## 주요 변경사항

### [추가] [Frontend/AgentStore/Workspace/Inbox] AI Hub 격상 1차 FE 구현
- **Agent Store** (`/agent-store` + `/agent-store/[slug]` 상세 페이지)
  - 3탭: Active Agents / Catalog / My Creations
  - 필터: 검색·capability·부서·공개범위·정렬
  - Mock 10개 Agent (6종 플랫폼 실제 반영: MISO Agent/Workflow, PAD, Python EC2, n8n, Native, Workspace RAG)
  - 헤더 🏪 Store 아이콘으로 진입

- **Workspace → Agent 붙이기 (P1)**
  - Workspace 설정 모달에 "Agents" 탭 추가
  - AgentPickerDialog로 설치한 Agent 다중 선택
  - localStorage 기반 매핑 (`ws_agents_{workspace_uuid}`)
  - 부작용 경고 UI (대화형 3개 이상, 스케줄 1개 이상, 활성 8개 이상)

- **Capability 다중 태그 체계 확정**
  - 💬 대화형 (chat) / ⚡ 실행형 (run) / 📅 스케줄 (scheduled) / ⏳ 비동기 (async)
  - 기존 3타입(Agent/Workflow/Knowledge) 폐기

- **알림 시스템 분리**
  - 📰 데일리 브리핑 신설 (기존 Bell 기능 이동)
  - 🔔 알림함 드로어 신설 (우측 슬라이드, 2탭: 내 알림/공지사항)
  - 📖 WIKI 외부 링크 분리
  - WhatsNew "새 기능" → "공지사항" 리브랜딩

### [수정] [Frontend/Sidebar] 워크스페이스 컨텍스트 유지
- **버그**: 워크스페이스 내 채팅 클릭 시 `/chat/[id]`로 이동하면서 `workspace_id` 쿼리 손실 → 사이드바가 전체 리스트로 되돌아감
- **해결**:
  - `SidebarHistoryItem` Link에 `workspace_id` 쿼리 포함
  - `Chat` 마운트 시 `replaceState`로 URL 동기화
  - 기존 `query` 처리 useEffect에서 `workspace_id` 보존

### [수정] [SAP RFC Bridge] 다중 시스템 지원 (DEV + PRD)
- **구조**: `SAPConnectionPool`을 시스템별 dict 구조로 전환
- **환경변수**: `.env`를 `SAP_DEV_*` / `SAP_PRD_*` 접두사 분리, 기본값 `SAP_DEFAULT_SYSTEM=prd`
- **API**: `/rfc/call`, `/rfc/ping`, `/rfc/password-init`에 `system` 필드 추가
- **Worker**: `reset_sap_password` MCP 도구와 ITSupportWorker 시스템 프롬프트에 dev/prd 선택 로직 반영
- **보정**: PRD IP `172.16.3.147`로 수정

### [수정] [PDF] `create_document_pdf` italic 폰트 미등록 버그
- **증상**: `Undefined font: malgungothicI` 오류로 subtitle 포함 PDF 생성 실패
- **원인**: MalgunGothic에 `I`/`BI` 스타일 미등록
- **해결**: `I`/`BI` 스타일을 regular/bold로 폴백 등록
- **2차 증상 해소**: LLM이 docx 성공에도 "전체 도구 오류" 오응답하던 현상도 함께 개선

### [수정] [IT VOC] 담당자 자동지정 직위 필터링 + 다중 부서 매핑
- **직위 필터**: `_get_dept_members()`에 `v_org_chart` JOIN, 직위 "파트장/책임"만 배정 (팀원/NULL 제외)
- **다중 부서**: `SYSTEM_CODE_TO_DEPT`(str) → `SYSTEM_CODE_TO_DEPTS`(tuple)로 변경
  - 예: 보안성 검토(code 14) = 보안기술팀 + 보안관리파트 공동 담당
- **DB**: `v_org_chart."직위"` 컬럼 추가 (DBA 작업)

### [추가] [Ops] NSSM 로그 수동 초기화 배치
- `C:\Services\logs\clear-logs.bat` 신규 생성
- `Clear-Content` in-place truncate로 서비스 재시작 없이 `backend-blue/green.log` 및 error 로그 비움
- NSSM 로테이션 미설정 상태에서의 수동 운영 수단 (deploy.log 제외)

### [수정] [Bedrock] 폴백 상태 영속화
- **문제**: 폴백 활성화 후 배포로 재시작되면 플래그 초기화 → 쿼터 회복 전 primary 재시도 위험
- **해결**: `_using_fallback` / `_restore_at`을 JSON 파일에 저장
- **경로**: Blue/Green 공용 `C:/Services/LFChatbot_prod/shared/fallback_state.json`
- **효과**: 재시작 후에도 KST 09:00 복구 예약 유지

### [수정] [MCP] 동시 스폰 수 4개로 제한
- **문제**: Windows에서 18개 MCP 서버 동시 스폰 시 `ExceptionGroup` 경쟁 실패 → 12개 서버 로드 실패
- **증상**: 2026-04-17 07:08 캐시 만료 후 재로딩 시 메일 도구 누락, MailWorker 응답